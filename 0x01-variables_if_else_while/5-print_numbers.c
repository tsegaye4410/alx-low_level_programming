#include <stdio.h>

/**
 * main - Entry Point
 * Description: 'print alphabet'
 * Return: Always 0 (suceess)
 */
int main(void)
{
	int c;

	for (c = 0; c < 10; c++)
		printf("%d", c);
	putchar('\n');
	return (0);
}
